// array
var train1 = ['wheat', 'barley', 'salt', 'potato', 'rocks']; 

// console log array index
console.log(train1[0]);
console.log(train1[1]);
console.log(train1[2]);
console.log(train1[3]);
console.log(train1[4]);

// alternatively loop through the array index
for(var i = 0; i < train1.length; i++) {
	console.log(train1[i]);
}

// arrays built-in methods
train1.push('oil');			// adds new item to end of array
var item = train1.pop();	// removes last item from end of array, can also save it into a variable





